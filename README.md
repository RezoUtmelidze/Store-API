# Store-API
Working Store API

---

Technologies that are used in this project Include: ASP.NET core, Entity Framework, SQL server.
Project is very optimized minimizing code to bare minimum while also keeping code practical and
easily maintainable. Features error handling and message logging. Features DTO models which make 
this API very safe

To test this project just simply run it in Visual Studio and use API via Swagger